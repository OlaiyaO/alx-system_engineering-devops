#!/usr/bin/env bash
# Displays the PID and process name of processes containing the word "bash" without using ps

for pid in /proc/*/cmdline; do
    if grep -q "bash" "$pid"; then
        echo "$(basename $(dirname $pid)) bash"
    fi
done
